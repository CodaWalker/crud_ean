    To work, you must run two scripts!
Start frontend:
     Relative path - crud-ean/backend,
     first start install npm i --save-dev,
     start - npm run frontend,
     port:4200

Start backend:
     Relative path - crud-ean/frontend,
     first start install npm i --save-dev,
     start - npm run backend,
     port:4000